import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ["firebasestorage.googleapis.com", "via.placeholder.com", "bizweb.dktcdn.net", "staging-api.dewcms.com"],
  },
};

export default nextConfig;
